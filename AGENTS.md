## Agent skills

### Issue tracker（事项跟踪）

本仓库的需求和 PRD 使用 GitHub Issues 管理。具体规则见 `docs/agents/issue-tracker.md`。

### Triage labels（分诊标签）

使用五个默认的 canonical triage labels。具体映射见 `docs/agents/triage-labels.md`。

### Domain docs（领域文档）

本仓库采用 multi-context 布局：根目录使用 `CONTEXT-MAP.md`，各上下文按需维护自己的 `CONTEXT.md` 和 ADR。具体规则见 `docs/agents/domain.md`。
