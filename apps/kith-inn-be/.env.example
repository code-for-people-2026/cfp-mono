# kith-inn-be env — copy to .env and fill real values (.env is gitignored).
# DeepSeek (be is the sole LLM host, Tech Spec §2):
DEEPSEEK_API_KEY=
# Use deepseek-chat (non-thinking). deepseek-v4-flash is a thinking model that
# rejects response_format and returns empty content at low token budgets.
DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# be ↔ cms + JWT:
JWT_SECRET=
CMS_BASE_URL=http://localhost:3304
CMS_INTERNAL_TOKEN=
