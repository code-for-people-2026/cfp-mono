# @cfp/miniapp-fe

> **⚠️ 已归档（ARCHIVED）。** Taro 前端应用（微信小程序 + H5），属于早期 `site` 试验阶段的产物。
> 代码保留作参考，但**已从 workspace 中移除**（见 `pnpm-workspace.yaml` 的排除项），
> 不再参与 `pnpm install` / `turbo` 构建 / CI / 部署。
> 如需恢复：去掉 `pnpm-workspace.yaml` 里对应的 `!apps/...` 排除项即可。

Taro + React 微信小程序前端演示，同时产出 H5 网页版。
